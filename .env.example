# ═══════════════════════════════════════════════════════════════════════════════
# CONFIGURACIÓN DEL PROYECTO TESIS
# ═══════════════════════════════════════════════════════════════════════════════

# 🗄️ CONFIGURACIÓN DE BASE DE DATOS
# ───────────────────────────────────────────────────────────────────────────────
DB_HOST=localhost                    # Host de PostgreSQL (localhost local, RDS/Cloud en producción)
DB_PORT=5432                         # Puerto de PostgreSQL
DB_USERNAME=postgres                 # Usuario de base de datos
DB_PASSWORD=password                 # Contraseña (CAMBIAR en producción)
DB_NAME=tesis_db                     # Nombre de la base de datos

# 🖥️ CONFIGURACIÓN DEL SERVIDOR
# ───────────────────────────────────────────────────────────────────────────────
NODE_ENV=development                 # development | production | test
PORT=3000                            # Puerto del servidor NestJS

# 🔐 CONFIGURACIÓN DE JWT
# ───────────────────────────────────────────────────────────────────────────────
JWT_SECRET=super-secret-key-123      # Clave JWT (GENERAR ALEATORIA en producción)
JWT_EXPIRATION=24h                   # Expiración de tokens

# 🌐 CONFIGURACIÓN DE CORS
# ───────────────────────────────────────────────────────────────────────────────
CORS_ORIGIN=http://localhost:3000    # Origen permitido (http://localhost:3000 local)
# En nube: https://tu-dominio.com

# 📁 CONFIGURACIÓN DE ARCHIVOS
# ───────────────────────────────────────────────────────────────────────────────
MAX_FILE_SIZE=100MB                  # Tamaño máximo de archivo
UPLOAD_DIR=./uploads                 # Directorio de uploads

# 🔍 CONFIGURACIÓN DE HERRAMIENTAS DE ANÁLISIS
# ───────────────────────────────────────────────────────────────────────────────
# Las herramientas están preinstaladas en Docker:
# • PMD (análisis estático)
# • SpotBugs (detección bugs)
# • Semgrep (análisis seguridad)
PMD_ENABLED=true
SPOTBUGS_ENABLED=true
SEMGREP_ENABLED=true

# 🔗 CONFIGURACIÓN DE GOOGLE OAUTH
# ───────────────────────────────────────────────────────────────────────────────
# Obtener en: https://console.developers.google.com
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback
# En nube: https://tu-dominio.com/auth/google/callback

# 📊 CONFIGURACIÓN DE LOGGING
# ───────────────────────────────────────────────────────────────────────────────
LOG_LEVEL=debug                      # debug | info | warn | error | fatal
LOG_FORMAT=json                      # json | pretty

# ═══════════════════════════════════════════════════════════════════════════════
# NOTAS IMPORTANTES
# ═══════════════════════════════════════════════════════════════════════════════

# 1. DESARROLLO LOCAL (docker-compose)
#    Las variables están preconfiguradas en docker-compose.yml
#    Puedes crear un .env local para sobrescribir

# 2. PRODUCCIÓN (Nube)
#    ⚠️ NO PONER SECRETOS AQUÍ
#    Usar "Secrets" del proveedor:
#    • Render: Environment / Secrets
#    • AWS: AWS Secrets Manager
#    • Azure: Key Vault

# 3. VARIABLES RECOMENDADAS EN PRODUCCIÓN
#    DB_PASSWORD: Generar contraseña fuerte (32 caracteres)
#    JWT_SECRET: Generar clave aleatoria (openssl rand -base64 32)
#    GOOGLE_CLIENT_SECRET: De Google Console

# 4. COMANDOS ÚTILES
#    # Generar JWT_SECRET
#    openssl rand -base64 32
#
#    # Generar contraseña aleatoria
#    python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#
#    # Copiar ejemplo para local
#    cp .env.example .env
PMD_ENABLED=true
SEMGREP_ENABLED=true
ESLINT_ENABLED=true
BANDIT_ENABLED=true

# Timeouts (en milisegundos)
TOOL_TIMEOUT=120000
ANALYSIS_TIMEOUT=300000